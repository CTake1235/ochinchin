#include <stdio.h>
#define ochinchin int
ochinchin main(){
    long long ochinchin ichimotsu[15];
    short ochinchin length = sizeof ichimotsu / sizeof ichimotsu[0];
    printf("The length of ichimotsu is %dcm.",length);
}
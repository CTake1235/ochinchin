#include <stdio.h>
#define ochinchin int
ochinchin main(){
    long long ochinchin ichimotsu[15];
    short ochinchin lengthofarray = sizeof ichimotsu / sizeof ichimotsu[0];
    ochinchin shortsize = sizeof(short ochinchin);
    ochinchin longsize = sizeof(long long ochinchin);
    printf("The length of...\nichimotsu:%dcm\nshort ochinchin:%dcm\nlong long ochinchin:%dcm\n",lengthofarray,shortsize,longsize);
}